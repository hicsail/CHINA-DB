To use the PullData class, add a file called 'auth.json' do the `config` 
directory with the following schema:

```json
{
  "BASE_KEY": "<your-airtable-base-key>",
  "API_KEY": "<your-airtable-api-key"
}
```